import React,{useState} from 'react';import {Theme} from '@astryxdesign/core';import {neutralTheme} from '@astryxdesign/theme-neutral';import LiveMonitor from './pages/LiveMonitor';import EntryLogs from './pages/EntryLogs';import {useLiveMonitor} from './hooks/useLiveMonitor';import './live.css';import './logs.css';export default function App(){const state=useLiveMonitor();const [page,setPage]=useState('live');return <Theme theme={neutralTheme}>{page==='live'?<LiveMonitor state={state} onNavigate={setPage}/>:<EntryLogs state={state} onNavigate={setPage}/>}</Theme>}
